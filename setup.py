from setuptools import setup

setup(
    name='shrimpgrad',
    author='Kevin Kenyon',
    license='MIT',
    version='0.0.1',
    python_requires='>=3.8',
    packages=['shrimpgrad'],
    install_requires=["graphviz", 'numpy']
)
