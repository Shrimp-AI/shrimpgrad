from setuptools import setup

setup(
    name='shrimpgrad',
    author='Kevin Kenyon',
    license='MIT',
    version='0.0.1',
    install_requires=[
        'python_version >= "3.8"',
    ],
    packages=['shrimpgrad']
)
